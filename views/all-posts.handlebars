    {{#each posts as | post | }}
    <div class="card">
    <h3 class="card-header">{{post.title}}</h3>
    {{!-- <br><h2 class="card-user">{{user.username}}</h2>
    <p class="card-body">{{post.body}}</p> --}}
    <p class="card-body">{{post.createdAt}}</p>
    </div>
    {{/each}}
