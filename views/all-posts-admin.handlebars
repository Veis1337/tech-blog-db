{{#each posts as | post | }}
<div class="card">
  <a href="/dashboard/edit/{{id}}">
    <div class="col-8 edit">{{post.title}}</div>
  </a>
 
  <p class="card-body" id="edit-body">{{post.body}}</p>
  <p class="card-body">{{post.createdAt}}</p>
  <p class="post-id">{{post.id}}</p>
</div>
{{/each}}